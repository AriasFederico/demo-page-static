import { globalData } from '../../data';

const { about } = globalData;
const { h2, p } = about;

export const About = () => {
	return (
		<div className='About' id='about'>
			<div></div>
		</div>
	);
};
